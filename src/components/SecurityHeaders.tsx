import { useEffect } from 'react';
import { SecurityHeadersManager } from './SecurityHeadersManager';

export function SecurityHeaders() {
  // Simplified - no headers manipulation to avoid conflicts
  return null;
}