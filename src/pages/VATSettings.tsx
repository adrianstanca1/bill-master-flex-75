
import React from 'react';
import { VATSchemeManager } from '@/components/VATSchemeManager';

const VATSettings: React.FC = () => {
  return (
    <div className="container mx-auto p-6">
      <VATSchemeManager />
    </div>
  );
};

export default VATSettings;
